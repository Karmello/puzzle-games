const initialState = {
  moves: 0
};

const eightQueensReducer = (state = initialState, action) => {
  
  switch (action.type) {

    default:
      return state;
  }
}

export default eightQueensReducer;